diff --git a/internal/helper/helper_darwin.go b/internal/helper/helper_darwin.go
index 3f6ef44..c10ce59 100644
--- a/internal/helper/helper_darwin.go
+++ b/internal/helper/helper_darwin.go
@@ -41,11 +41,11 @@ func (c *DarwinConfigurator) Prerequisites() error {
	var err error

-	c.wireGuardBinary, err = pathWithFallBacks("wg", "/usr/local/bin/wg", "/opt/homebrew/bin/wg", "/usr/bin/wg")
-	if err != nil {
-		return fmt.Errorf("look for wg: %w", err)
-	}
-
-	c.wireGuardGoBinary, err = pathWithFallBacks("wireguard-go", "/usr/local/bin/wireguard-go", "/opt/homebrew/bin/wireguard-go", "/usr/bin/wireguard-go")
-	if err != nil {
-		return fmt.Errorf("look for wireguard-go: %w", err)
-	}
+	c.wireGuardBinary = "__WG_PATH__"
+	if _, err = os.Stat(c.wireGuardBinary); err != nil {
+		return fmt.Errorf("look for wg: %w", err)
+	}
+
+	c.wireGuardGoBinary = "__WIREGUARD_GO_PATH__"
+	if _, err = os.Stat(c.wireGuardGoBinary); err != nil {
+		return fmt.Errorf("look for wireguard-go: %w", err)
+	}
